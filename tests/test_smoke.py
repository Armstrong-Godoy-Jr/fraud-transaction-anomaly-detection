"""
Smoke test — confirms the test suite itself is wired up correctly.
Replace/extend with real tests as each phase adds actual code to src/.
"""


def test_smoke():
    """Sanity check: if this fails, something is fundamentally broken
    in the test environment, not in project logic."""
    assert True
